@using Community3.Models
@model Gender

<select id="Gender" name="Gender">
    @foreach (Gender gender in Enum.GetValues(typeof(Gender)))
    {
        <option value="@gender" @(Model == gender ? "selected=\"selected\"" : "")>@gender</option>
    }
</select> 