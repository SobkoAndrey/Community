@model IEnumerable<Community3.Models.Audio>

@foreach (var audio in Model)
{
    <div>@audio.Label</div>
    <audio controls>
        <source src="@Url.Content(audio.Path)" type="audio/mpeg">
    </audio>
    <br />
}
